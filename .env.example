# AI Newsletter Bot - Environment Variables

# Authentication
AUTH_PASSWORD=your_secure_password_here

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# Logging (optional)
LOG_LEVEL=INFO

# Caching (optional)
CACHE_TTL=1800

# Output directory (usually auto-set, override if needed)
OUTPUT_DIR=./output

# For Vercel deployments:
# - Set these in Vercel dashboard under Settings → Environment Variables
# - Do NOT commit sensitive values to .env or .env.local
